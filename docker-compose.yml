version: '3.5'
services:
  dev:
    user: root
    build:
      dockerfile: Dockerfile
      context: .
    volumes:
      - ./:/helix.pipelines/
    container_name: helix.bSights.cql_spark_engine
    environment:
      LOGLEVEL: DEBUG
